<div class="container-fluid">
  <h1>🧰 Request to add <%= @tool.name %> to your Toolbox</h1>

  <%= simple_form_for [@tool, @booking] do |f| %>
    <%= f.input :starting_date, html5: true %>
    <%= f.input :end_date, html5: true %>
    <%= f.submit %>
  <% end %>

  <%# <%= link_to "Cancel", tools_path %>
</div>
