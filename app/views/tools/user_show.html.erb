<%= @tool.name %>
<%= @tool.description %>


<p><%= link_to "Back to tools", tools_path %></p>

<p><%= link_to "Delete tool", @tool, method: :delete %></p>

<p><%= link_to "Edit tool details", edit_tool_path(@tool) %></p>


<%# Link to request to book a tool %>
<%= link_to "Book this tool",  new_tool_booking_path(@tool), {:style=>'color: gray;'}%>

<div class="card-product">
  <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" />
  <div class="card-product-infos">
    <h2>Product name</h2>
    <p>Product description with <strong>relevant info</strong> only.</p>
  </div>
</div>

 <div class="card-info">
      <ul>
          <li>
            <%=link_to t.name, tool_path(t) %>
          </li>
          <li>
            <%= t.tool_type %>
          </li>
          <li>
            <%= t.price %>
          </li>
          <li>
           <%= link_to "Book this tool",  new_tool_booking_path(t), {:style=>'color: gray; padding-left: 1rem;'}%>
          </li>
      </ul>
    </div>
