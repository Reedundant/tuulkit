

      <%= @tool.name %>
      <%= @tool.description %>
      <%# <%= link_to "Delete", %>

<%# #         data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>

<%# <div>
  <p><%= link_to "Edit this restaurant", edit_restaurant_path(@restaurant) %>
  <p><%= link_to "Back to tools", tools_path %></p>

  <p><%= link_to "Delete tool", @tool, method: :delete %></p>

  <p><%= link_to "Edit tool details", edit_tool_path(@tool) %></p>
